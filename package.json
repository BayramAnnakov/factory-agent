{
  "name": "factory-agent",
  "version": "0.1.0",
  "type": "module",
  "description": "Autonomous Linear-to-PR agent. Delegate an issue, get a pull request.",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "@linear/sdk": "^37.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.12"
  }
}
